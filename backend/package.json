{
  "scripts": {
    "prestart": "node -e \"const fs = require('fs'); if (process.env.FIREBASE_CREDENTIALS) { fs.mkdirSync('dist/config', { recursive: true }); fs.writeFileSync('dist/config/firebase-service-account.json', process.env.FIREBASE_CREDENTIALS); fs.writeFileSync('firebase-services-account.json', process.env.FIREBASE_CREDENTIALS); console.log('Successfully wrote Firebase configurations safely.'); }\"",
    "build": "esbuild src/server.ts --bundle --platform=node --target=node16 --outfile=dist/server.js --external:express --external:cors --external:dotenv --external:@google/generative-ai --external:axios --external:firebase-admin --external:zod --external:module-alias",
    "start": "node -r module-alias/register dist/server.js"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "axios": "^1.16.1",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "firebase-admin": "^13.10.0",
    "module-alias": "^2.3.4",
    "zod": "^4.4.3"
  },
  "_moduleAliases": {
    "@": "dist"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
